package be.ugent.objprog.ugentopoly.layout.tileCards;

import javafx.scene.image.ImageView;

public interface TileCards {
    void setPion(ImageView imageView, double spelerLeftAnchor);
    void removePion(ImageView imageView);
}
